// @ts-check

/**
 * Attribute change handlers for PixColorSchemeSwitcher.
 *
 * This component has no observed attributes at this time.
 * Add entries here when attributes are introduced:
 *
 * @example
 * ```js
 * export default {
 *   scheme(_oldValue, newValue) {
 *     this.applyColorScheme(newValue ?? 'system');
 *   },
 * };
 * ```
 */
export default {};
