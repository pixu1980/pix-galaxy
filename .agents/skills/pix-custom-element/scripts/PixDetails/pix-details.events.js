// @ts-check

/**
 * DOM event handlers for PixDetails. The decorator builds `handle<Type>Event`
 * methods on the prototype and registers `this` as the EventListener for each
 * key during `connectedCallback`.
 *
 * PixDetails relies on the native `<details>` toggle behaviour, so no custom
 * DOM events are needed. Add entries here to react to native events.
 */
export default {};
