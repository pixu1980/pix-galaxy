/**
 * @fires PixSplitter#splitter-resize
 * Fired while a panel is being resized.
 * @type {CustomEvent<{ ratios: number[], index: number }>}
 */

/**
 * @fires PixSplitter#splitter-resize-end
 * Fired when resize interaction ends.
 * @type {CustomEvent<{ ratios: number[], index: number }>}
 */
